import React from 'react';
import PageWrapper from './PageWrapper';

const NotFoundPage = () => (
  <PageWrapper> No catalog information is available on this page </PageWrapper>

);

export default NotFoundPage;
